parameters:
    # Needed only when you use "rules" block below
    level: 5
    paths:
        - src

    # Sometimes needed
    autoload_files:
        - autoload.php

    # Prevents scanning given folders/files
    excludes_analyse:
        - src/Core/ShortFunctions.php